pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Construyendo el proyecto'
                // Agrega los comandos necesarios para construir tu proyecto
            }
        }
        stage('Test') {
            steps {
                echo 'Ejecutando pruebas'
                // Agrega los comandos necesarios para ejecutar pruebas
            }
        }
        stage('Deploy') {
            steps {
                echo 'Desplegando la aplicación'
                // Agrega los comandos necesarios para desplegar tu aplicación
            }
        }
    }
}
